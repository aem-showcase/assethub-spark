# Adobe I/O Console workspace config (contains credentials — never commit)
SparkAppbuilderPOC-*.json

# App Builder build output
dist/

# Local env (also covered by root .gitignore)
.env
.env.*
!.env.example
